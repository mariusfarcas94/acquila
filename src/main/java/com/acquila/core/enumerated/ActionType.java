package com.acquila.core.enumerated;

/**
 * Enum that holds the types of the actions a user can perform on an acquisition.
 */
public enum ActionType {

    //todo(mfarcas) - these might change according to business logic.
    CREATE,
    UPDATE,
    APPROVE,
    FINALIZE
}
